{
  "inport": 15000,          // listen this port
  "inhost": "",             // listen on this host/IP
  "outport": 10000,         // forward to this port
  "outhost": ["localhost"], // forward to this host
  "workers": 0              // max number of accepted incoming request (0-no limit)
}